# تقرير المعالجة الهندسية — الذاكرين

**التاريخ:** 23 أغسطس 2026
**النطاق:** تنفيذ نتائج تدقيق مؤكد بعد فحص الكود الفعلي، مع المحافظة على سلوك الميزات الحالية وعقود القنوات والحفظ.

## خط الأساس

| الفحص | قبل المعالجة |
|---|---|
| اختبارات Flutter | 35 ناجحة |
| التحليل الساكن | 0 أخطاء، 0 تحذيرات، 68 ملاحظة info |
| بناء APK التجريبي | ناجح |
| بناء APK للإصدار | ناجح |

## النتائج المؤكدة والمعالجة

| المشكلة | السبب الجذري | المعالجة | الحفاظ على السلوك |
|---|---|---|---|
| نسخ احتياطية متتبعة | وجود `data_manager.dart.orig` و`settings_section.dart.orig` في Git، مع تكرار أنواع حية وبيانات قديمة. | حذفت النسختين وأضفت `*.orig` إلى `.gitignore`. | لم يكن أي منهما جزءًا من مسار بناء Flutter أو الاستيراد. |
| دورة حياة شاشة القبلة | اشتراك بث المستشعر لم يكن يُحفظ أو يُلغى، واستدعاءات الموقع غير المتزامنة كانت قد تصل إلى واجهة محذوفة. | حفظت الاشتراك وإلغاؤه في `dispose` وأضفت حراس `mounted` ومعالجة فشل الموقع. | بقيت قناة القبلة والحوار والنصوص والاتجاه المحسوب كما هي. |
| اتجاه قبلة ثابت | كان النص السفلي يعرض `201°` حرفيًا بغض النظر عن موقع المستخدم. | ربطت النص بالاتجاه المحسوب فعليًا واستخرجت الحساب إلى دالة قابلة للاختبار. | لا تغير في معادلة الاتجاه أو قناة المستشعر. |
| تداخل مسؤوليات Android | تضمن `MainActivity` تنفيذ نمط اهتزاز المسبحة مباشرةً إضافة إلى مسؤوليات القنوات الأخرى. | نقلت نمط الاهتزاز إلى `TasbihFeedbackManager`. | اسم قناة المسبحة وطريقة الاستدعاء بقيتا كما هما. |
| أذونات تخزين قديمة | كان Manifest يطلب التخزين القديم مع عدم وجود استهلاك للتخزين الخارجي في مسار التحديث. | أزلت `READ/WRITE_EXTERNAL_STORAGE` و`requestLegacyExternalStorage`. | بقيت صلاحية تثبيت التحديث، والصلاحيات اللازمة للأذان، والصوت، والإشعارات. |
| استخدام سياق بعد async | ظهرت حالات مؤكدة في الخيرة وصفحة التعريف. | أضفت حراس lifecycle بعد عمليات النسخ والمشاركة وفتح الروابط. | رسائل النجاح والفشل للمستخدم لم تتغير. |

## التحقق المنفذ

| الفحص | النتيجة | التغطية |
|---|---|---|
| `flutter test` | 36 ناجحة | منطق المسبحة، قناة الاهتزاز، أوقات الصلاة، البحث، القرآن، بطاقة الصلاة القادمة، حساب القبلة. |
| `flutter analyze --no-fatal-infos` | 0 أخطاء، 0 تحذيرات، 64 ملاحظة info | انخفضت الملاحظات من 68؛ لا تبقى تحذيرات `BuildContext` غير المتزامن أو API الخاص لشاشة القبلة. |
| `flutter build apk --debug` | ناجح | ترجمة Flutter وAndroid مع الطبقة الأصلية الجديدة. |
| `flutter build apk --release` | ناجح | بناء إصدار موقّع محليًا وفق إعداد المشروع الحالي. |
| `git diff --check` | ناجح | لا توجد أخطاء مسافات في التغييرات. |

## قرارات مؤجلة عمدًا

| الموضوع | القرار | السبب |
|---|---|---|
| تفكيك الملفات الكبيرة | مؤجل | الحجم وحده ليس دليلًا كافيًا لإعادة هيكلة واسعة؛ لا تُجرى إعادة كتابة سليمة بلا خطر وظيفي أو قياس حاجة. |
| ترقيات التبعيات وGradle/AGP/Kotlin | مؤجلة إلى مرحلة توافق مستقلة | توجد حزم أحدث، وبعضها major؛ الترقية العشوائية ستوسع نطاق المخاطر. |
| إعداد Firebase المتتبع | يحتاج مراجعة مالك مشروع Firebase | ملف التهيئة مطلوب للبناء الحالي؛ ينبغي تقييد مفتاح API من لوحة Google وإتاحة بديل سري في CI قبل حذفه من Git. لا تُعرض أي قيمة حساسة هنا. |
| اختبار Android الميداني | مطلوب لاحقًا | لا يوجد جهاز أو محاكي متصل. ما يزال اختبار Doze، الصوت، الاهتزاز المادي، الإقلاع وتفاعل صلاحيات الشركات المصنعة يحتاج جهازًا فعليًا. |

## التقييم النهائي

أصبح المشروع أنظف وأقل تعرضًا لأخطاء lifecycle وتسريب المستمعات وتراكم ملفات النسخ الاحتياطية، مع فصل محدود ومبرر لمسؤولية Android. لم تُحذف ميزة ولم تتغير عقود القنوات أو منطق الأذان أو انتقالات تسبيحة الزهراء. تبقى ترقية التبعيات واختبارات Android الميدانية مرحلة مستقلة ينبغي تنفيذها ببيئة جهاز حقيقية ومراجعة حسابات Firebase.
