# تقرير تنفيذ الإصلاحات — Islamic

**المشروع:** `techtouchAI/Islamic`  
**حالة Git:** تم تعديل النسخة المحلية فقط؛ لم يتم إنشاء commit أو push.  
**نطاق التنفيذ:** دمج متطلبات البرومبتين وتنفيذ إصلاحات الأذان، الموقع، المنطقة الزمنية، الاستعادة، الصوت، الصلاحيات، المسبحة، الاختبارات وCI.

## النتيجة العامة

تم تنفيذ الإصلاحات المطلوبة مباشرة على النسخة المحلية من المشروع. أصبح الحساب والعرض والجدولة يعتمدون على نفس النتيجة النهائية بدل وجود مسارات مستقلة قد تعرض وقتًا وتجدول وقتًا آخر. كما أضيفت state machine قابلة للاختبار لتسبيحة الزهراء، وأصبح مسار Android Native يحفظ alarms ويستعيدها بعد أحداث النظام الأساسية.

## أوقات الصلاة والموقع

أضيف نموذج `PrayerLocation` موحد يميز صراحة بين الموقع الحي عبر GPS، المدينة التي اختارها المستخدم، آخر موقع GPS محفوظ، والمدينة الافتراضية. عند فشل GPS لا تُعرض المدينة الافتراضية على أنها GPS حي، ولا يُستبدل اختيار المدينة الذي حدده المستخدم بإحداثيات أخرى.

تم توحيد `PrayerTimesSection` و`HomeSection` و`PrayerTimesService` حول `PrayerSchedule`. النتيجة الواحدة تحتوي على الوقت المدني المحلي المستخدم للعرض، ووقت UTC المستخدم للمنبه، والموقع ومصدره والإزاحة اليدوية. المدن العراقية تستخدم الإزاحة المدنية الثابتة UTC+3، بينما GPS وGPS cache يتبعان سياسة منطقة الجهاز. هذا يمنع استخدام `DateTime.toLocal()` الخاص ببيئة الجهاز لعرض مدينة عراقية عند تشغيل الجهاز في منطقة زمنية مختلفة.

تم تعديل محرك PrayTimes الجعفري ليتيح النتائج الخام القابلة للتحقق، ويتجاهل القيم غير المنتهية بدل تحويل `NaN` إلى `00:00`. كما أصبح منتصف الليل الشرعي محسوبًا على أنه منتصف المدة بين غروب اليوم وفجر اليوم التالي، مع التحقق من صلاحية طرفي المدة قبل الإخراج.

## جدولة الأذان والإشعارات

أصبح `PrayerAlarmService` جسرًا واحدًا إلى `AdhanNativeManager`. يحفظ المدير الأصلي سجل كل alarm ووقت العرض المدني وUTC وإزاحة timezone ومصدر اعتماد timezone، ويفصل pre-alert عن تشغيل الأذان الفعلي. عند توفر صلاحية exact alarm يستخدم `setAlarmClock`، وعند عدم توفرها يستخدم fallback آمنًا مع تسجيل واضح بدل الفشل الصامت.

أضيفت استعادة السجلات بعد الإقلاع، وتحديث الحزمة، وتغير التاريخ، وتغير timezone. عند تغير timezone تُعاد حسابات alarms المعتمدة على GPS من الوقت المدني المحفوظ مع الإزاحة الحالية، بينما تبقى أوقات المدن الثابتة مرتبطة بسياسة المدينة. كما أضيفت إعادة إنشاء pending intents بعد الاستعادة لتفادي تكرار أو بقاء جدولة قديمة.

أضيفت واجهة حالة لصلاحية exact alarm وfull-screen intent، مع فتح إعداد النظام عند الحاجة. ويستمر الصوت من المصدر الموحد `android/app/src/main/res/raw/adhan.mp3` بعد إزالة النسختين المتطابقتين `azan5.mp3` من assets وres/raw.

## المسبحة وتسبيحة الزهراء

تمت إعادة بناء منطق تسبيحة الزهراء في `TasbihZahraState` كـstate machine نقية قابلة للاختبار. التسلسل هو:

| المرحلة | الذكر | العدد |
|---|---|---:|
| 1 | الله أكبر | 34 |
| 2 | الحمد لله | 33 |
| 3 | سبحان الله | 33 |

كل نقرة تزيد العداد مرة واحدة فقط. عند إكمال المرحلة ينتقل العداد إلى الصفر وتظهر رسالة المرحلة التالية، ويُنفذ اهتزاز انتقال إضافي. عند إكمال سبحان الله تُزاد الدورات المكتملة ويبدأ العداد من جديد عند الله أكبر. الحفظ مستقل لتقدم تسبيحة الزهراء، مع حفظ المرحلة والعداد والدورات والعداد التراكمي. كما تم حصر معالجة النقر في handler واحد وإضافة حماية من عمليات التحميل والحفظ المتداخلة.

## الاختبارات والتحقق

| الفحص | النتيجة |
|---|---|
| `flutter pub get` باستخدام Flutter 3.47.1 | ناجح بعد رفع قيد `intl` إلى `^0.20.3` ليتوافق مع `flutter_localizations` |
| `flutter analyze --no-fatal-infos` | ناجح، بلا errors أو warnings؛ بقيت 68 ملاحظة lint من مستوى info فقط في أجزاء قديمة من المشروع |
| `flutter test --reporter expanded` | ناجح: **29 اختبارًا** |
| اختبارات الموقع والحساب | تغطي مصدر المدينة، فشل GPS، UTC+3، القيم غير المنتهية، ومنتصف الليل الجعفري |
| اختبارات المسبحة | تغطي 34/33/33، التصفير عند الانتقال، إكمال الدورة، العداد التراكمي وإعادة ضبط المرحلة |
| `flutter build apk --debug` | ناجح، وتم إنتاج APK قابل للبناء |
| `git diff --check` | ناجح، بلا مسافات أو أخطاء diff |

تم تحديث CI ليشغل `dart format` ثم `flutter analyze --no-fatal-infos` ثم `flutter test` قبل بناء APK. كما تم تعديل release workflow ليعمل عند تغييرات `lib/**` و`test/**` و`android/**` وملفات الاعتمادات بدل الاقتصار على تغييرات `pubspec.yaml` فقط.

## ملاحظات متبقية

التحقق المحلي تم على مستوى unit tests وDart analyze وAndroid debug build. لم يتم اختبار إطلاق alarm على جهاز Android فعلي أو قياس GPS والاهتزاز والصوت تحت Doze؛ هذه تحتاج جهازًا أو محاكيًا فعليًا. كذلك يعرض Flutter تحذيرات توافق مستقبلية لأن Gradle 8.14 وAGP 8.11.1 وKotlin 2.2.20 ستحتاج إلى ترقية مستقبلية قبل أن يتوقف دعمها، لكن البناء الحالي ناجح ولم أقم بترقيتها لتجنب تغيير نطاق المشروع دون تحقق مستقل.

لم يتم تشغيل release build محليًا لأن ملف keystore وأسرار التوقيع غير موجودة في بيئة الفحص؛ مسار release في CI ما زال يستخدم أسرار GitHub الخاصة بالمستودع.

## الملفات الرئيسية المتأثرة

تمت إضافة نماذج `PrayerLocation` و`PrayerSchedule` و`TasbihZahraState`، وتحديث محرك الحساب وخدمة الصلاة وواجهة الصلاة والصفحة الرئيسية وخدمة الجسر وطبقات Android Native والمسبحة والاختبارات وCI. كما أزيلت الاعتمادات والمسارات الميتة، وملفات workflow المؤقتة `*.orig` و`*.rej`، وملف الاختبار اليدوي غير المفيد.

**النتيجة:** الكود الحالي قابل للتحليل والاختبار والبناء debug، ولم يتم رفعه إلى GitHub أو إنشاء commit بناءً على طلب التنفيذ المحلي فقط.
